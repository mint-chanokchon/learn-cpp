
    * std::max() หาค่าที่มากที่สุดระหว่าง 2 ตัวแปร

    * std::min() หาค่าที่น้อยที่สุดระหว่าง 2 ตัวแปร

    * pow() ยกกำลัง

    * sqrt() หาสแควรูท

    * abs() absolute เปลี่ยนค่าลบเป็นบวก

    * round() ปัดเศษเป็น 0 

    * ceil() ปัดเศษขึ้น

    * floor() ปันเศษลง